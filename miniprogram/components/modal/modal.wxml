<view class="popup" hidden="{{!isShow}}" bind:tap="close">
	<view class="popup-container" catch:tap="none">
		<view class="popup-title">
			{{title}}
			<text bind:tap="close" style="font-size: 48rpx">×</text>
		</view>
		<view class="popup-con">
			<slot/>
		</view>
	</view>
</view>

