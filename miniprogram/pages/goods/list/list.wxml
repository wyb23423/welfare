<!-- 小程序tab页无法传参, 使用两个页面构建 -->
<view>
    <image 
        wx:if="{{!!ad}}" 
        src="{{ad}}"
        mode="aspectFill"
        style="height: 40vw; margin-bottom: 20rpx; width: 100%"
    />
    <app-list isGoods="{{1}}" id="list" />
</view>
