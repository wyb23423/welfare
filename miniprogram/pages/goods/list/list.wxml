<!-- 小程序tab页无法传参, 使用两个页面构建 -->
<view>
    <view wx:if="{{!!ad}}"  style="height: 59vw; padding: 20rpx">
        <image  src="{{ad}}" class="fill-box card" style="padding: 0" />
    </view>
    <app-list isGoods="{{1}}" id="list" />
</view>
