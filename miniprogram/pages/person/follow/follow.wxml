<view>
    <business-item wx:for="{{list}}" wx:key="key" data="{{item}}">
        <view class="btn">
            <button hover-class="btn-tap" class="iconfont icon-delete" data-index="{{item.index}}" bind:tap="delete" />
        </view>
    </business-item>
</view>