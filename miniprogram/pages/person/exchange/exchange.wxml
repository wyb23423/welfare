<import src="/template/list_item/list_item.wxml" />
<view>
	<navigator
	 wx:for="{{exchanges}}"
	 wx:key="{{item.id}}"
	 hover-class="none"
	 url="/pages/details/details?id={{item.id}}&isGoods=1"
	>
		<template is="goods_item" data="{{...item}}" />
	</navigator>
</view>

