<view class="info">
	<view class="avator">
		<open-data type="userAvatarUrl" />
	</view>
	<view style="margin: 10rpx 0">
		<open-data type="userNickName" class="name" />
	</view>
	<view style="color: #fff">ID: {{username}}</view>
	<text wx:if="{{isindex && hasCommodity}}" class="btn" bind:tap="authentication">社区认证</text>
	<navigator class="point" url="../info/info" wx:if="{{isindex}}">···</navigator>
</view>

